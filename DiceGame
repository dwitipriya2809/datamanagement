import random

def roll_dice():
    die1 = random.randint(1, 6)
    die2 = random.randint(1, 6)
    total_value = die1 + die2

    print("Rolling dice...")
    print("Die 1:", die1)
    print("Die 2:", die2)
    print("Total value:", total_value)

roll_dice()
